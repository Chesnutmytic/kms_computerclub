<?php
session_start();require_once '../config/koneksi.php';if(!isset($_SESSION['id_user'])){header('Location: ../login.html');exit;}$id=(int)($_GET['id']??0);$stmt=$conn->prepare("SELECT a.*,p.nama_lengkap FROM ALUR_PEMBELAJARAN a JOIN PENGGUNA p ON p.id_user=a.id_user WHERE a.id_alur=:id AND a.status='Published'");$stmt->execute([':id'=>$id]);$alur=$stmt->fetch();if(!$alur){header('Location: alur_belajar.php');exit;}$stmt=$conn->prepare("SELECT am.judul_dokumen,am.kategori,am.deskripsi,am.file_path FROM DETAIL_ALUR d JOIN ARSIP_MATERI am ON am.id_arsip=d.id_arsip WHERE d.id_alur=:id AND am.status='Published' ORDER BY am.judul_dokumen");$stmt->execute([':id'=>$id]);$materi=$stmt->fetchAll();include '../includes/header_portal.html'; ?>
<main class="container py-5"><a href="alur_belajar.php" class="btn btn-sm btn-outline-secondary mb-4">← Kembali ke Alur Belajar</a><div class="card border-0 shadow-sm"><div class="card-body p-4"><span class="badge text-bg-primary mb-2"><?= htmlspecialchars($alur['tingkat_level']?:'Semua Level') ?></span><h1 class="h3"><?= htmlspecialchars($alur['nama_alur']) ?></h1><p class="text-muted mb-0">Dibuat oleh <?= htmlspecialchars($alur['nama_lengkap']) ?> · <?= htmlspecialchars($alur['tgl_dibuat']) ?></p></div></div><h2 class="h5 mt-5 mb-3">Materi dalam Alur</h2><div class="list-group shadow-sm"><?php foreach($materi as $m): ?><div class="list-group-item"><div class="d-flex justify-content-between align-items-center gap-3"><div><strong><?= htmlspecialchars($m['judul_dokumen']) ?></strong><div class="small text-muted"><?= htmlspecialchars($m['kategori']?:'Umum') ?></div></div><?php if($m['file_path']): ?><a class="btn btn-sm btn-outline-primary" target="_blank" rel="noopener" href="../<?= htmlspecialchars(ltrim($m['file_path'],'/')) ?>">Buka</a><?php endif; ?></div></div><?php endforeach;if(!$materi): ?><div class="list-group-item text-muted">Belum ada materi published dalam alur ini.</div><?php endif; ?></div></main>
<?php include '../includes/footer_portal.html'; ?>
